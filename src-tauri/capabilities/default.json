{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults only. No filesystem, shell or HTTP plugins are exposed to the UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
